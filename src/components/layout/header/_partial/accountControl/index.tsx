import React from 'react';
import { Button } from '../../../../ui';


export const AccountControl: React.FC = (props: object ): JSX.Element => {
    return (
        <Button className="account__control">Login</Button>
    );
}